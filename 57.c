#include<stdio.h>
#include<conio.h>
int swap (int,int);
int main()
{
int i,n,j,a;
printf("enter two numbers n & a");
scanf("%d %d",&n,&a);
rintf("after swap values are n=%d a=%d",x,y);
}
swap(n,a);
}
int swap(int x,int y)
{
    int t;
    t=x;
    x=y;
    y=t;
    printf("after swap values n=%d a=%d",x,y);
}
